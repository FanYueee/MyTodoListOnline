services:
  mytodolist:
    build:
      context: .
    image: mytodolistonline:latest
    ports:
      - "${TODO_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      TODO_DATA_DIR: /app/data
    volumes:
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
